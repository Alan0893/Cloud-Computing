apiVersion: v1
kind: Service
metadata:
  name: hw9-webserver
  namespace: default
  labels:
    app: hw9-webserver
spec:
  type: LoadBalancer
  selector:
    app: hw9-webserver
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
